


pub fn serialize() {

    let target: Option<String> = Some("hello_world".to_string());
    //
    // let encoded: Vec<u8> = bincode::serialize(&target).unwrap();
    //
    // let decoded: Option<String> = bincode::deserialize(&encoded[..]).unwrap();
    // assert_eq!(target, decoded);
}